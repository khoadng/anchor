name: anchor
description: An overlay positioning calculation package for Flutter, inspired by Floating UI.
version: 0.0.6
homepage: https://github.com/khoadng/anchor
repository: https://github.com/khoadng/anchor
issue_tracker: https://github.com/khoadng/anchor/issues
topics: 
  - positioning
  - anchoring
  - overlay
  - floating-ui

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.6
