name: anchor_tour
description: Guided tours and coach marks built on top of flutter_anchor.
version: 0.0.1
homepage: https://github.com/khoadng/anchor
repository: https://github.com/khoadng/anchor
issue_tracker: https://github.com/khoadng/anchor/issues
topics:
  - tour
  - coach-mark
  - onboarding
  - overlay

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_anchor: ^0.0.13
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
